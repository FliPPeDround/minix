{
  "name": "@minix/monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "ci": "vp check && vp run -r test && vp run -r build",
    "dev": "vp run website#dev",
    "dev:demo": "vp run demo#dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20260707.2",
    "vite": "catalog:vite",
    "vite-plus": "catalog:vite"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.13.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
